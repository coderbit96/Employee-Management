import { NextRequest } from "next/server"; import { apiError, apiOk, getRequestContext, validationError } from "@/lib/api/response"; import { getCurrentUser } from "@/lib/auth/session"; import { withdrawLeaveSchema } from "@/lib/validation/leave-request"; import { LeaveRequestServiceError, requestLeaveCancellation } from "@/services/leave-request-service";
export async function POST(request: NextRequest, { params }: { params: Promise<{ id: string }> }) { const context=getRequestContext(request); const actor=await getCurrentUser(); if(!actor)return apiError("UNAUTHENTICATED","Please sign in.",{status:401,requestId:context.requestId}); const parsed=withdrawLeaveSchema.safeParse(await request.json().catch(()=>null)); if(!parsed.success)return validationError(parsed.error,context.requestId); try{return apiOk(await requestLeaveCancellation((await params).id,parsed.data.reason,actor,context),{requestId:context.requestId});}catch(error){return error instanceof LeaveRequestServiceError?apiError(error.code,error.message,{status:error.status,requestId:context.requestId}):apiError("CANCELLATION_FAILED","Unable to request cancellation.",{status:500,requestId:context.requestId});}}
