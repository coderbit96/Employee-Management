import { NextRequest } from "next/server"; import { apiError,apiOk,getRequestContext,validationError } from "@/lib/api/response"; import { getCurrentUser } from "@/lib/auth/session"; import { createLeaveRequestSchema } from "@/lib/validation/leave-request"; import { LeaveRequestServiceError,updateLeaveRequest } from "@/services/leave-request-service";
export async function PATCH(request:NextRequest,{params}:{params:Promise<{id:string}>}){const context=getRequestContext(request);const actor=await getCurrentUser();if(!actor)return apiError("UNAUTHENTICATED","Please sign in.",{status:401,requestId:context.requestId});const parsed=createLeaveRequestSchema.safeParse(await request.json().catch(()=>null));if(!parsed.success)return validationError(parsed.error,context.requestId);try{return apiOk(await updateLeaveRequest((await params).id,parsed.data,actor,context),{requestId:context.requestId});}catch(error){return error instanceof LeaveRequestServiceError?apiError(error.code,error.message,{status:error.status,requestId:context.requestId}):apiError("LEAVE_UPDATE_FAILED","Unable to update leave.",{status:500,requestId:context.requestId});}}
