import { redirect } from "next/navigation";
import { getCurrentUser } from "@/lib/auth/session";
import { getWorkforceSummary } from "@/services/report-service";
export default async function ReportsPage() { const user = await getCurrentUser(); if (!user) redirect("/login"); if (!["SUPER_ADMIN", "ADMIN", "HR", "MANAGER"].includes(user.role)) return <p className="rounded-lg border bg-white p-5">Your role cannot view workforce reports.</p>; const summary = await getWorkforceSummary(user); const metrics = [{ label: "Headcount", value: summary.headcount }, { label: "Active users", value: summary.activeUsers }, { label: "Inactive users", value: summary.inactiveUsers }, { label: `Attendance ${summary.workDate}`, value: summary.attendanceToday }, { label: "Not checked in", value: summary.absentOrNotCheckedIn }, { label: "Attendance exceptions", value: summary.attendanceExceptions }, { label: "Pending leave", value: summary.pendingLeave }, { label: "Open payroll items", value: summary.openPayrollItems }, { label: "Paid records", value: summary.paidPayments }]; return <div className="space-y-6"><section><p className="text-sm font-medium text-emerald-800">{summary.scope.replace("_", " ")}</p><h1 className="mt-2 text-2xl font-semibold text-slate-950">Workforce report</h1></section><section className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{metrics.map((metric) => <article key={metric.label} className="rounded-lg border border-slate-200 bg-white p-5 shadow-sm"><p className="text-sm text-slate-600">{metric.label}</p><p className="mt-2 text-3xl font-semibold text-slate-950">{metric.value}</p></article>)}</section></div>; }
